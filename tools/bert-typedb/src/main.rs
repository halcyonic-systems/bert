//! CLI entry point for the BERT→TypeDB transpiler.
//!
//! Usage will be filled in as the library API stabilises. For now, this is
//! a placeholder that prints a banner — exists so that `cargo build` and
//! `cargo check` exercise the binary target.

fn main() {
    println!("bert-typedb 0.1.0 — under construction (see issue #37)");
}
